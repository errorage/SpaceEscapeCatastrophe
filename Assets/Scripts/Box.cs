using UnityEngine;
using System.Collections;

public class Box : MonoBehaviour {

	// Use this for initialization
	void Start () {
		World.boxes.Add(this);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
